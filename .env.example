# Amped OpenClaw Plugin Configuration

# =============================================================================
# CONFIGURATION OPTION 1: Using evm-wallet-skill (RECOMMENDED)
# =============================================================================
# If you're already using evm-wallet-skill, you DON'T need any additional 
# configuration! This plugin automatically detects and uses:
#   - EVM_WALLETS_JSON (or WALLET_CONFIG_JSON)
#   - EVM_RPC_URLS_JSON (or RPC_URLS_JSON)
#
# Just make sure those are set in your environment, and you're good to go!

# =============================================================================
# CONFIGURATION OPTION 2: Plugin-Specific (Only if NOT using evm-wallet-skill)
# =============================================================================
# Uncomment and set these ONLY if you don't have evm-wallet-skill configured:

# Wallet configuration (JSON map of walletId -> {address, privateKey})
# AMPED_OC_WALLETS_JSON='{
#   "main": {
#     "address": "0xYourWalletAddress",
#     "privateKey": "0xYourPrivateKey"
#   }
# }'

# RPC URLs for supported chains
# AMPED_OC_RPC_URLS_JSON='{
#   "ethereum": "https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY",
#   "arbitrum": "https://arb-mainnet.g.alchemy.com/v2/YOUR_KEY",
#   "base": "https://base-mainnet.g.alchemy.com/v2/YOUR_KEY",
#   "sonic": "https://rpc.soniclabs.com"
# }'

# =============================================================================
# OPTIONAL: Additional Plugin Configuration
# =============================================================================

# Operation mode: "execute" (default, signs transactions) or "prepare" (returns unsigned txs)
# AMPED_OC_MODE=execute

# Enable dynamic SODAX config (fetches fresh config from API instead of using static)
# AMPED_OC_SODAX_DYNAMIC_CONFIG=false

# Policy limits for security (JSON)
# AMPED_OC_LIMITS_JSON='{
#   "default": {
#     "maxSlippageBps": 100,
#     "maxSwapInputUsd": 10000,
#     "maxBorrowUsd": 50000,
#     "allowedChains": ["ethereum", "arbitrum", "base", "sonic"]
#   }
# }'

# =============================================================================
# OPTIONAL: SODAX Partner Configuration (for fee sharing)
# =============================================================================

# Partner wallet address to receive partner fees from swaps/bridges
# SODAX_PARTNER_ADDRESS=0xYourPartnerWalletAddress

# Partner fee percentage in basis points (e.g., 10 = 0.1%, 50 = 0.5%)
# If not set, uses default partner fee rate
# SODAX_PARTNER_FEE_BPS=10

# =============================================================================
# OPTIONAL: SODAX API Configuration (for intent history queries)
# =============================================================================

# API Base URL - Use canary for pre-release features, production for stable
# SODAX_API_URL=https://canary-api.sodax.com  # Pre-release / testing
# SODAX_API_URL=https://api.sodax.com         # Production

# API Key (if required by your SODAX plan)
# SODAX_API_KEY=
